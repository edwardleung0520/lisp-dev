# Common Lisp 開發環境 - GitHub Codespaces

這個專案為 Common Lisp 提供 GitHub Codespaces 開發環境，使用 SBCL 作為編譯器。

## 如何使用

1. 點選「Code」按鈕 → 「Codespaces」 → 「Create codespace on main」
2. 在開啟的 VS Code Web 中編輯並執行 `.lisp` 程式
3. 在終端輸入：
   ```bash
   sbcl --script hello.lisp

